# requirements.txt - Core Pipeline Dependencies
# Use requirements/base.txt + service-specific for full installs
# Core: streaming + utils + monitoring (lightweight startup)

# Install base first, then detector-specific ML
# pip install -r requirements/base.txt
# pip install -r requirements/ml.txt  # for detector

# Core from base.txt (included via pip -r requirements/base.txt):
# kafka-python, confluent-kafka, redis, pyyaml, aiohttp, etc.
# prometheus-client, python-json-logger

# Detector needs ML (install separately):
# pip install -r requirements/ml.txt  # scikit-learn, xgboost, joblib, numpy, pandas

# Cloud (conditional - install if needed):
# pip install boto3 azure-mgmt-network==25.0.0 azure-identity google-cloud-compute

# API service (separate):
# pip install -r requirements/api.txt  # fastapi, uvicorn

# Collector (pcap - heavy, separate):
# pip install -r requirements/collector.txt  # scapy, pyshark, nfstream

# Quickstart: pip install -r requirements/base.txt && pip install -r requirements/ml.txt
# Full dev: + -r requirements/api.txt -r requirements/collector.txt
# Cloud: Add cloud SDKs as needed (boto3, azure-*, google-cloud-*)
# Test: + pytest pytest-asyncio
